Main codes: Chiller_RL_noTarget_sequentialCritic.py

Before run the main codes, put the file in 'gymenv' into gym environment.

the objective function is defined as: rewards = -SP/RT, thus the more closer to zero the rewards is, the better the control result. 
